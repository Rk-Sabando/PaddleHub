import { test } from "@playwright/test";

test.skip("user can join an open match and see slots update", async ({ page: _page }) => {
  // TODO: seed an open match, click Join, assert optimistic UI + final state.
});
