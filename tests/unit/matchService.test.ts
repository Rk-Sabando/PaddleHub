import { describe, it, expect } from "vitest";

// TODO: Set up a test DB (sqlite or Neon branch) and exercise matchService.
// For now, this file is a placeholder so vitest discovers the suite.

describe("matchService", () => {
  it("placeholder — wire up test DB", () => {
    expect(true).toBe(true);
  });
});
