// TODO: Email digests + match-start reminders.
// Suggest a queue (BullMQ on Upstash Redis, or Inngest) for scheduled jobs.
export async function sendMatchReminder(_matchId: string) {
  // implementation pending
}
